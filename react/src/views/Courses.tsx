import styled from "styled-components";

const Courses = () => {
    return (
        <StyledSection>
            Courses
        </StyledSection>
    )
}

export default Courses;

const StyledSection = styled.section`
    
`