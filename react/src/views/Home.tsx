import styled from "styled-components";

const Home = () => {
    return (
        <StyledSection>
            Home
        </StyledSection>
    )
}

export default Home;

const StyledSection = styled.section`
    margin-top: calc(8dvh + 20px);
`