import styled from "styled-components";

const Main = () => {
    return (
        <StyledSection>
            Main
        </StyledSection>
    )
}

export default Main;

const StyledSection = styled.section`
    margin-top: calc(8dvh + 20px);
`